apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: nginx-service-mtls
  namespace: {{ .Release.Namespace }}
spec:
  host: nginx-service.{{ .Release.Namespace }}.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
